"""MCP server process — the query/reasoning interface.

Thin application layer over the domain services. It is downstream of the always-on
monitoring path and never becomes the source of truth for calculations.
"""
